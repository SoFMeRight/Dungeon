apiVersion: v1
kind: Service
metadata:
  name: sideporch-camera
spec:
  type: ExternalName
  externalName: sideporch.prplanit.internal
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
