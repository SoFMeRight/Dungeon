apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: anirra-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-great-sea
  dataFrom:
  - extract:
      key: apps/anirra
