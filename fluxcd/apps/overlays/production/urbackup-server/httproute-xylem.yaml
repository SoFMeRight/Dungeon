apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: urbackup-server-xylem
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Backup Services (Fairy Bottle🧚)"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
    - name: phloem-gateway
      namespace: prelude-of-light
  hostnames:
    - "backups.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: urbackup-server
          port: 55414
