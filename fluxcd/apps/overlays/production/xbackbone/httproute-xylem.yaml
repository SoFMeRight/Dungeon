apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: xbackbone-xylem
  annotations:
    gatus.home-operations.com/endpoint: |
      group: "Tools & Utilities (Tingle Tuner🔧)"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
    - name: phloem-gateway
      namespace: prelude-of-light
  hostnames:
    - "xbackbone.pcfae.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: xbackbone
          port: 80
