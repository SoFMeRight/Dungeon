apiVersion: v1
kind: Service
metadata:
  name: truenas-webui
spec:
  type: ExternalName
  externalName: gringotts.prplanit.internal
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
