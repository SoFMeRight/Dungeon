apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: temple-of-time

resources:
  - ../../../base/hubzilla
  - httproute.yaml

patches:
  - path: externalsecret-patch.yaml
  - path: pvc-store-patch.yaml
  - path: statefulset-mariadb-patch.yaml
  - path: deployment-hubzilla-patch.yaml
  - path: deployment-cron-patch.yaml
