apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: photoprism-x-secret
spec:
  secretStoreRef:
    name: openbao-pedestal-of-time
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: photoprism-x-smb-secret
spec:
  secretStoreRef:
    name: openbao-pedestal-of-time
