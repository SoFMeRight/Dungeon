apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: calcom-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-precisionplanit
  dataFrom:
  - extract:
      key: apps/calcom
