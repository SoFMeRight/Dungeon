apiVersion: apps/v1
kind: Deployment
metadata:
  name: frappe-suite-queue-long
spec:
  revisionHistoryLimit: 5
  replicas: 1
  selector:
    matchLabels:
      app: frappe-suite-queue-long
  template:
    metadata:
      labels:
        app: frappe-suite-queue-long
    spec:
      serviceAccountName: frappe-suite-queue-long
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: queue-long
          image: PLACEHOLDER_ERPNEXT_IMAGE
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          command:
            - bench
            - worker
            - --queue
            - long,default,short
          volumeMounts:
            - name: sites-data
              mountPath: /home/frappe/frappe-bench/sites
            - name: logs-data
              mountPath: /home/frappe/frappe-bench/logs
      volumes:
        - name: sites-data
          persistentVolumeClaim:
            claimName: PLACEHOLDER_SITES_PVC
        - name: logs-data
          persistentVolumeClaim:
            claimName: PLACEHOLDER_LOGS_PVC
