apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-mariadb
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: frappe-suite-mariadb
---
apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-redis-cache
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: frappe-suite-redis-cache
---
apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-redis-queue
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: frappe-suite-redis-queue
---
apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-sites
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: backend
  selector:
    app: frappe-suite-sites
---
apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-websocket
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: 9000
      name: websocket
  selector:
    app: frappe-suite-websocket
---
apiVersion: v1
kind: Service
metadata:
  name: frappe-suite-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: frappe-suite-frontend
