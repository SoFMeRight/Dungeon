apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - mariadb.yaml
  - mariadb-externalsecret.yaml
  - statefulset.yaml
  - externalsecret.yaml
  - service.yaml
  - configmap-scripts.yaml
  # Scheduled console commands — all suspend:true (opt-in); enable per feature in the overlay.
  - cronjob-stop-all.yaml
  - cronjob-invoice.yaml
  - cronjob-export.yaml
