apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: frigate-secret
spec:
  secretStoreRef:
    name: vault-zeldas-letter
    kind: ClusterSecretStore
  target:
    name: frigate-secret
  dataFrom:
  - extract:
      key: apps/frigate
