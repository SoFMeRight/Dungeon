apiVersion: v1
kind: ServiceAccount
metadata:
  name: hasteward
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: hasteward
rules:
  # CNPG Cluster CRs — watch, get, list, patch annotations (fence, reconciliationLoop)
  - apiGroups: ["postgresql.cnpg.io"]
    resources: ["clusters"]
    verbs: ["get", "list", "watch", "patch"]
  # CNPG Cluster status subresource — realign targetPrimary to cancel a stuck failover
  # after deadlock-recover (settlePrimary). A subresource needs its own rule; the
  # "clusters" patch verb above does NOT cover it.
  - apiGroups: ["postgresql.cnpg.io"]
    resources: ["clusters/status"]
    verbs: ["get", "patch", "update"]
  # CNPG Backup CRs — create native backups
  - apiGroups: ["postgresql.cnpg.io"]
    resources: ["backups"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # VolumeSnapshots — escrow the PVC before an in-place deadlock-recover (create + poll
  # readyToUse), and clean up old escrows. VolumeSnapshotClasses + StorageClasses are read
  # to auto-discover the snapshot class matching the PVC provisioner.
  - apiGroups: ["snapshot.storage.k8s.io"]
    resources: ["volumesnapshots"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: ["snapshot.storage.k8s.io"]
    resources: ["volumesnapshotclasses"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["storageclasses"]
    verbs: ["get", "list", "watch"]
  # MariaDB CRs — watch, get, list, patch annotations
  - apiGroups: ["k8s.mariadb.com"]
    resources: ["mariadbs"]
    verbs: ["get", "list", "watch", "patch"]
  # Hasteward CRDs — full access
  - apiGroups: ["clinic.hasteward.prplanit.com"]
    resources: ["backuprepositories", "backuppolicies"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["clinic.hasteward.prplanit.com"]
    resources: ["backuprepositories/status"]
    verbs: ["get", "update", "patch"]
  # Pods — exec for dump/restore, get/list for triage, create/delete for heal helpers
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "delete"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  # Secrets — read database credentials, TLS certs, repo passwords
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list"]
  # PVCs — triage disk usage checks
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list"]
  # Events — emit Kubernetes events
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
  # StatefulSets — scale subresource for Galera node healing
  - apiGroups: ["apps"]
    resources: ["statefulsets/scale"]
    verbs: ["get", "update"]
  # Leases — leader election (operator mode)
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: hasteward
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: hasteward
subjects:
  - kind: ServiceAccount
    name: hasteward
    namespace: PLACEHOLDER_NAMESPACE
