apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: thelounge
spec:
  revisionHistoryLimit: 5
  serviceName: thelounge
  replicas: 1
  selector:
    matchLabels:
      app: thelounge
  template:
    metadata:
      labels:
        app: thelounge
    spec:
      serviceAccountName: thelounge
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: thelounge
          image: docker.io/linuxserver/thelounge:PLACEHOLDER_IMAGE_TAG
          # LSIO/s6-overlay image: starts root PID 1 so s6 chowns /config and setuidgid-drops to
          # abc (uid 1000); can't run non-root without a rebuild. RO-root blocked (s6 writes /run
          # + /config). Partial: drop ALL + s6's file-ownership caps + no-privesc.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - CHOWN
                - DAC_OVERRIDE
                - FOWNER
                - SETUID
                - SETGID
          env:
            - name: TZ
              value: "PLACEHOLDER_TIMEZONE"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            # LSIO thelounge persists to /config (the official image's THELOUNGE_HOME=/var/opt/thelounge
            # is not used here) — the PVC must land on /config or all state stays on ephemeral overlayfs.
            - name: config
              mountPath: /config
        - name: gluetun
          image: docker.io/qmcgaw/gluetun:PLACEHOLDER_IMAGE_TAG
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_ADMIN
                - CHOWN
                - DAC_OVERRIDE
                - FOWNER
                - MKNOD
                - SETUID
                - SETGID
              drop:
                - ALL
          env:
            - name: VPN_SERVICE_PROVIDER
              value: "PLACEHOLDER_VPN_PROVIDER"
            - name: SERVER_REGIONS
              value: "PLACEHOLDER_SERVER_REGIONS"
            - name: FIREWALL_VPN_INPUT_PORTS
              value: "PLACEHOLDER_VPN_INPUT_PORTS"
            - name: OPENVPN_USER
              valueFrom:
                secretKeyRef:
                  name: thelounge-vpn-secret
                  key: vpn_user
            - name: OPENVPN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: thelounge-vpn-secret
                  key: vpn_password
            - name: DOT
              value: "off"
            - name: DNS_ADDRESS
              value: "PLACEHOLDER_DNS_ADDRESS"
            - name: HEALTH_VPN_DURATION_INITIAL
              value: "60s"
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
  volumeClaimTemplates:
    - metadata:
        name: config
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: PLACEHOLDER_STORAGE_CLASS
        resources:
          requests:
            storage: 2Gi
