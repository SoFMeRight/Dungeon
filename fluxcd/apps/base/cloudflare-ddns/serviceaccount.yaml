apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflare-ddns
# Makes no Kubernetes API calls (only outbound HTTPS to the Cloudflare API), so it needs
# no projected API token — dropping it removes a credential from the pod.
automountServiceAccountToken: false
