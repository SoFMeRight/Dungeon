apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-gateway-basic-auth
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-zeldas-letter
  target:
    name: loki-gateway-basic-auth
    creationPolicy: Owner
  data:
    - secretKey: ".htpasswd"
      remoteRef:
        key: apps/loki
        property: BASIC_AUTH_HTPASS
