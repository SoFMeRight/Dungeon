# DERIVED: pod targetPorts from HTTPRoute backendRefs targeting this namespace.
# Inputs: HTTPRoutes → Services (targetPort resolution) → EndpointSlices (named port resolution)
# Key: (gateway parentRef -> discovered ingress-gateway class) + resolved targetPort
# Regenerate; do not hand-edit.
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-gateway-ingress-phloem
spec:
  selector:
    matchLabels:
      policy.prplanit.com/ingress: "true"
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/prelude-of-light/sa/phloem-gateway-istio
      to:
        - operation:
            ports: ["3000", "3001", "5050", "8080", "8090", "8480", "9001"]
