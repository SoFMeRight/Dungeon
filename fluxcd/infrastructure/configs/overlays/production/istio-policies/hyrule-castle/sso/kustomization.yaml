apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# SSO ext-authz on the cell-membrane (public) gateway. Shared infra first, then one file
# per gated host — this list doubles as the index of what's behind SSO. Onboard an app =
# add its <host>.yaml + one line here.
resources:
  - _gateway-header-strip.yaml
  - z2m.arbitorium.com.yaml
  - books.optcp.com.yaml
