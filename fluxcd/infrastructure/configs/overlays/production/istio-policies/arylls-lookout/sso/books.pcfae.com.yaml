# calibre-web  ·  SSO required  ·  tier: operator-only  (admits: operator)
#
# Self-contained: this file IS the complete access rule for books.pcfae.com.
# One file per gated host — find an app's rule by its filename, no cross-referencing.
#   require-login (CUSTOM) → oauth2-proxy proves an SSO session + injects the role header
#   authz (DENY)          → deny unless the injected role is in `admits` below
# notPaths /oauth2/* : the oauth2-proxy sign-in/callback/static paths must bypass the gate
#   (else the login page would have to authenticate itself). Served by the oauth2-proxy-signin
#   HTTPRoute (books.pcfae.com/oauth2/* → oauth2-proxy).
# Widen access by adding roles to notValues (operator is the floor):
#   operator-only : [operator]   admin: [operator,admin]   trusted: [operator,admin,trusted]   guest: [operator,admin,trusted,guest]
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: sso-books-require-login
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: xylem-gateway
  action: CUSTOM
  provider:
    name: oauth2-proxy
  rules:
    - to:
        - operation:
            hosts:
              - books.pcfae.com
            notPaths:
              - /oauth2/*
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: sso-books-authz
spec:
  selector:
    matchLabels:
      gateway.networking.k8s.io/gateway-name: xylem-gateway
  action: DENY
  rules:
    - to:
        - operation:
            hosts:
              - books.pcfae.com
            notPaths:
              - /oauth2/*
      when:
        - key: request.headers[x-auth-request-groups]
          notValues:
            - operator     # admits: operator
