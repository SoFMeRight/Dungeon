apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-redirect-phloem
  namespace: prelude-of-light
spec:
  parentRefs:
    - name: phloem-gateway
      namespace: prelude-of-light
      sectionName: http
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
