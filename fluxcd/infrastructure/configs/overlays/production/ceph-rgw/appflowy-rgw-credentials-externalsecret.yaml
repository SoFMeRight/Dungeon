apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: appflowy-rgw-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-zeldas-letter
  target:
    name: appflowy-rgw-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 5m
  dataFrom:
    - extract:
        key: rgw/appflowy
        conversionStrategy: Default
        decodingStrategy: None
