apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-rgw-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-zeldas-letter
  target:
    name: nextcloud-rgw-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 5m
  dataFrom:
    - extract:
        key: rgw/nextcloud
        conversionStrategy: Default
        decodingStrategy: None
