apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-rgw-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-zeldas-letter
  target:
    name: loki-rgw-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 5m
  dataFrom:
    - extract:
        key: rgw/loki
        conversionStrategy: Default
        decodingStrategy: None
