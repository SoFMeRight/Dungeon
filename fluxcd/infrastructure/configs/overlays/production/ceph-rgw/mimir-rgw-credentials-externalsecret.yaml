apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mimir-rgw-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-zeldas-letter
  target:
    name: mimir-rgw-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 5m
  dataFrom:
    - extract:
        key: rgw/mimir
        conversionStrategy: Default
        decodingStrategy: None
