apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: wallmaster
resources:
  - cnp-default-deny-ingress.yaml
  # Egress allow (inert until egress default-deny is enabled for the namespace).
  - cnp-egress-anubis.yaml
