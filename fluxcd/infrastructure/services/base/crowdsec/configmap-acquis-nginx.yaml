apiVersion: v1
kind: ConfigMap
metadata:
  name: crowdsec-acquis-nginx
data:
  10-nginx-loki.yaml: |
    source: loki
    url: PLACEHOLDER_LOKI_URL
    limit: 1000
    query: |
      {service_name="nginx-extras"} |= "GET"
    labels:
      type: nginx
    wait_for_ready: 5m
