apiVersion: v1
kind: Service
metadata:
  name: netbird-management
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 10000
      name: http
  selector:
    app: netbird
    component: management
