apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openbao-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: PLACEHOLDER_STORAGE_CLASS
  resources:
    requests:
      storage: 10Gi
