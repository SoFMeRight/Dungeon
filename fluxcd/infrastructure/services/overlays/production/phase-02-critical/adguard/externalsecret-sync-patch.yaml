apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: adguardhome-sync-config
spec:
  secretStoreRef:
    name: vault-zeldas-letter
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: apps/adguardhome-sync
