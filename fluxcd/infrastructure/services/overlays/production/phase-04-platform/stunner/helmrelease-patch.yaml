apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stunner
spec:
  targetNamespace: king-of-red-lions
  chart:
    spec:
      # renovate: datasource=helm depName=stunner registryUrl=https://l7mp.io/stunner
      version: "1.2.1"
