apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitlab-praefect-credentials
spec:
  secretStoreRef:
    name: openbao-precisionplanit
    kind: ClusterSecretStore
  data:
  - secretKey: password
    remoteRef:
      key: apps/gitlab
      property: praefect_password
