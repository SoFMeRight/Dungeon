---
# Docker Hub proxy cache — all routing handled by nginx proxy
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-proxy-cache-docker
  annotations:
    gatus.home-operations.com/endpoint: |
      # A registry answers 404 at the root; /v2/ returning an auth challenge is
      # what proves the proxy cache is actually serving.
      url: https://docker.cr.pcfae.com/v2/
      conditions:
        - "[STATUS] == 401"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "docker.cr.pcfae.com"
  rules:
    - backendRefs:
        - name: harbor-proxy-cache-proxy
          port: 8080
---
# GitHub Container Registry proxy cache
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-proxy-cache-ghcr
  annotations:
    gatus.home-operations.com/endpoint: |
      # A registry answers 404 at the root; /v2/ returning an auth challenge is
      # what proves the proxy cache is actually serving.
      url: https://ghcr.cr.pcfae.com/v2/
      conditions:
        - "[STATUS] == 401"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "ghcr.cr.pcfae.com"
  rules:
    - backendRefs:
        - name: harbor-proxy-cache-proxy
          port: 8080
---
# Quay.io proxy cache
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-proxy-cache-quay
  annotations:
    gatus.home-operations.com/endpoint: |
      # A registry answers 404 at the root; /v2/ returning an auth challenge is
      # what proves the proxy cache is actually serving.
      url: https://quay.cr.pcfae.com/v2/
      conditions:
        - "[STATUS] == 401"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "quay.cr.pcfae.com"
  rules:
    - backendRefs:
        - name: harbor-proxy-cache-proxy
          port: 8080
---
# LinuxServer.io proxy cache
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor-proxy-cache-lscr
  annotations:
    gatus.home-operations.com/endpoint: |
      # A registry answers 404 at the root; /v2/ returning an auth challenge is
      # what proves the proxy cache is actually serving.
      url: https://lscr.cr.pcfae.com/v2/
      conditions:
        - "[STATUS] == 401"
spec:
  parentRefs:
    - name: xylem-gateway
      namespace: arylls-lookout
  hostnames:
    - "lscr.cr.pcfae.com"
  rules:
    - backendRefs:
        - name: harbor-proxy-cache-proxy
          port: 8080
